#!/bin/sh
# Simple wrapper for lutron_cli.py monitor function

# Get the directory where this script is located
SCRIPT_DIR=$(dirname "$0")

# Execute the Python script with monitor command
exec "$SCRIPT_DIR/lutron_cli.py" monitor "$@" 