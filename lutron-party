#!/bin/sh
# Simple wrapper for lutron_cli.py party function

# Get the directory where this script is located
SCRIPT_DIR=$(dirname "$0")

# Execute the Python script with party command
exec "$SCRIPT_DIR/lutron_cli.py" party "$@" 